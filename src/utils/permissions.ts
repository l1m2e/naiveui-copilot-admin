export function hasPermi(_str: string) {
  // const { userInfo } = storeToRefs(useUserStore())
  // if (userInfo.value.user?.is_superuser) return true
  // return userInfo.value?.role?.permission_list?.some((v) => {
  //   return v === str
  // }) || false
  return true
}
